# Assets

Drop your media files here:

- `profile/yann.jpg` for your profile picture
- `logos/*.png` for company logos referenced in `data/experience.yml`
- `badges/*.png` for certification badges referenced in `data/certifications.yml`

Keep file names in sync with YAML paths.
